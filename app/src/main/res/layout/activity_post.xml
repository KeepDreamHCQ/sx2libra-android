<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/post_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/surface_background">

    <com.suixin.sx2libra.web.LibraWebViewRefreshLayout
        android:id="@+id/post_refresh"
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <WebView
            android:id="@+id/post_web_view"
            android:layout_width="match_parent"
            android:layout_height="match_parent" />
    </com.suixin.sx2libra.web.LibraWebViewRefreshLayout>

    <ProgressBar
        android:id="@+id/post_progress"
        style="?android:attr/progressBarStyleHorizontal"
        android:layout_width="match_parent"
        android:layout_height="3dp"
        android:max="100"
        android:progressTint="@color/libra_primary"
        android:indeterminateTint="@color/libra_primary"
        android:visibility="gone" />

    <TextView
        android:id="@+id/post_error"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:gravity="center"
        android:padding="24dp"
        android:textAppearance="@style/TextAppearance.Sx2libra.Error"
        android:text="@string/web_page_load_error"
        android:visibility="gone" />

    <FrameLayout
        android:id="@+id/post_snapshot_overlay"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@color/surface_background"
        android:clickable="true"
        android:focusable="true"
        android:importantForAccessibility="noHideDescendants"
        android:visibility="gone">

        <ImageView
            android:id="@+id/post_snapshot_image"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:importantForAccessibility="no"
            android:scaleType="fitStart"
            android:visibility="gone" />

        <ProgressBar
            android:id="@+id/post_snapshot_checking"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:visibility="gone" />

        <LinearLayout
            android:id="@+id/post_snapshot_fallback_panel"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:gravity="center"
            android:orientation="vertical"
            android:padding="24dp"
            android:visibility="gone">

            <TextView
                android:id="@+id/post_snapshot_fallback_text"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:gravity="center"
                android:text="@string/web_snapshot_fallback" />

            <Button
                android:id="@+id/post_snapshot_retry"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="8dp"
                android:text="@string/web_snapshot_retry" />
        </LinearLayout>
    </FrameLayout>
</FrameLayout>
